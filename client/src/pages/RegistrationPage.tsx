import LoginRegistration from "../components/LoginRegistraton/LoginRegistration"

type Props = {}

const RegistrationPage = (props: Props) => {
    return (
        <LoginRegistration isLogin={false} />
    )
}

export default RegistrationPage